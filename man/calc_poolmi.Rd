% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_poolmi.R
\name{calc_poolmi}
\alias{calc_poolmi}
\title{Pool estimates across imputations using Rubin's rules}
\usage{
calc_poolmi(
  est,
  se,
  data = NULL,
  df = c("1987", "1999"),
  conf = 0.95,
  n = NULL,
  k = NULL
)
}
\arguments{
\item{est}{Point estimates, one per imputation. With \code{data}, the name of
the estimate column instead.}

\item{se}{Standard errors, one per imputation; positive and non-missing.
With \code{data}, the name of the standard error column.}

\item{data}{Optional data frame with one row per imputation. When given,
\code{est} and \code{se} are column names.}

\item{df}{Degrees-of-freedom method: \code{"1987"} (Rubin 1987; the SAS PROC
MIANALYZE default) or \code{"1999"} (Barnard and Rubin 1999; matches
\code{mice::pool()} and PROC MIANALYZE with EDF). Default \code{"1987"}.}

\item{conf}{Confidence level, in (0, 1). Default \code{0.95}.}

\item{n}{Sample size of each imputed dataset. Required for \code{df = "1999"}.}

\item{k}{Number of parameters in the fitted model. Required for
\code{df = "1999"}.}
}
\value{
A 1-row data frame: \code{pool.est}, \code{pool.se} (square root of total
variance), \code{lower}/\code{upper} confidence bounds, and two-sided \code{p.val}.
}
\description{
Pools estimates and standard errors across imputed datasets by Rubin's
rules, returning the pooled estimate, its confidence interval, and
two-sided p-value. Supply the per-imputation results as numeric vectors,
or as columns of a stacked data frame with one row per imputation.
}
\examples{
# vector interface
calc_poolmi(est = c(-2.57, -2.11, -1.85, -2.07, -2.13),
            se  = c( 1.05,  0.91,  0.92,  0.85,  0.90))

# data frame interface (one row per imputation)
imp_result <- data.frame(
  IMPNO = 1:5,
  est   = c(-2.57, -2.11, -1.85, -2.07, -2.13),
  SE    = c( 1.05,  0.91,  0.92,  0.85,  0.90)
)
calc_poolmi(est = "est", se = "SE", data = imp_result)

# see the package vignette for the full workflow using ADMI dataset,
# a cross-check against mice::pool(), and the equivalent SAS PROC
# MIANALYZE calls
}
\references{
Rubin, D.B. (1987). \emph{Multiple Imputation for Nonresponse in Surveys}. Wiley.

Barnard, J. and Rubin, D.B. (1999). Small-sample degrees of freedom with
multiple imputation. \emph{Biometrika}, 86(4), 948-955.
}
