% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict_param.R
\name{dict_param}
\alias{dict_param}
\title{Extract PARAM metadata from ADaM datasets}
\usage{
dict_param(x)
}
\arguments{
\item{x}{A data frame, or a named list of data frames.}
}
\value{
For a data frame: a data frame of unique \code{PARAMN}/\code{PARAMCD}/
\code{PARAM} combinations, sorted by \code{PARAMN} when available. For a named
list: a named list of such data frames.
}
\description{
Summarizes the unique \code{PARAM}, \code{PARAMCD}, and \code{PARAMN} combinations in a
dataset, as found in parameter-based ADaM data (ADLB, ADVS, ADTTE, and
the like). \code{PARAM} is required; \code{PARAMCD} and \code{PARAMN} are included when
present. Given a named list, datasets without a \code{PARAM} column are
dropped with a message naming them; if none remain, an error is raised.
}
\details{
In ADaM, each analysis parameter is identified three ways:
\describe{
\item{PARAM}{Full parameter description, e.g. \code{"Albumin (g/L)"}.}
\item{PARAMCD}{Short parameter code, 8 characters or fewer, e.g. \code{"ALB"}.}
\item{PARAMN}{Numeric parameter code, typically used for sorting.}
}
}
\examples{
\dontrun{
# single ADaM data frame
adlb <- haven::read_xpt("adlb.xpt")
adlb_param <- dict_param(adlb)

# named list of ADaM data frames
adam <- list(adlb = haven::read_xpt("adlb.xpt"),
             advs = haven::read_xpt("advs.xpt"))
adlb_param <- dict_param(adam)
}
}
