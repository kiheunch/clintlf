% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_stat.R
\name{fmt_stat}
\alias{fmt_stat}
\title{Format a descriptive statistics cell}
\usage{
fmt_stat(
  x = NULL,
  ...,
  n = 1L,
  inner_n = 1L,
  bracket = "(",
  sep = ", ",
  na = "-",
  eps = TRUE
)
}
\arguments{
\item{x}{Lead value shown before the brackets. Numeric or character
coercible to numeric; \code{NULL} for a cell with no lead such as
\code{"min, max"}.}

\item{...}{One or more vectors for the bracketed group, in display order,
each the same length as \code{x}. Numeric or character coercible to numeric.}

\item{n}{Decimal places for the lead. Default \code{1}.}

\item{inner_n}{Decimal places for the bracketed values. Default \code{1}.}

\item{bracket}{Opening bracket: \code{"("}, \code{"["}, or \code{""} for none. The
closing bracket follows. Default \code{"("}.
#FIXME: dont make it match.arg, Allow either empty character or two characters, e.g., "()" or "(]" so it can be used for inclusive/exclusive range.
Perhaps default is NULL which does () when there is \code{x}, and "" when there is not.  But user can provide "()" or "" or even "(]"}

\item{na}{Shown in place of a missing value, e.g. \code{"-"} for the SD of a
single observation. \code{NaN} displays the same way. Default \code{"-"}.}

\item{eps}{Apply the floating-point correction of \code{\link[=fmt_round]{fmt_round()}}.
Default \code{TRUE}.}
}
\value{
Character vector, one cell per element, e.g. \code{"23.5 (4.57)"}.
}
\description{
The general cell builder: a lead value followed by a bracketed group,
\code{"x (a, b)"}, rounding half away from zero. One function covers the usual
summary cells: \code{estimate (LowerCI, UpperCI)}, \code{mean (SD)},
\code{median (Q1, Q3)}, or \verb{min, max} with no lead and no brackets. Without user manualling going through tedious string concatenation.
}
\examples{
fmt_stat(3.14159, 1.23456, 5.6789, n = 2, inner_n = 2)   # estimate (95\% CI): "3.14 (1.23, 5.68)"
fmt_stat(23.456, 4.5678,         , n = 1, inner_n = 2)   # mean (SD):         "23.5 (4.57)" 
fmt_stat(NULL, 0.123, 9.876)                             # min, max:          "0.1, 9.8"
fmt_stat(c(12.3, 15.8), c(4.56, NA))                     # vectorized:        "12.3 (4.6)" "15.8 (-)"
}
\seealso{
\code{\link[=fmt_round]{fmt_round()}}
}
