% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_pval.R
\name{fmt_pval}
\alias{fmt_pval}
\title{Format a p-value with a reporting threshold}
\usage{
fmt_pval(x, n = 2L, alpha = "0.05", space = FALSE)
}
\arguments{
\item{x}{Numeric vector of p-values in [0, 1]. \code{NA} prints as \code{"NA"}.}

\item{n}{Decimal places: a non-negative integer, scalar or one per element
of \code{x}. Default \code{2}.}

\item{alpha}{Reporting threshold, in (0, 1); scalar or same length as
\code{x}. Pass as character to keep trailing zeros in the label (\code{"0.10"}
prints \code{"<0.10"}). Default \code{"0.05"}.}

\item{space}{Insert a space after \code{"<"}. Default \code{FALSE}.}
}
\value{
Character vector of formatted p-values.
}
\description{
Displays values below \code{alpha} as \code{"<alpha"} and rounds the rest half away
from zero.
}
\details{
A value just above the threshold can round to the threshold itself: 0.051
with \code{n = 2} prints \code{"0.05"} next to \code{"<0.05"}. Use a larger \code{n} near the
threshold if that ambiguity matters.
}
\examples{
fmt_pval(0.055)
fmt_pval(c(0.051, 0.001, NA), n = 3, alpha = "0.05")
fmt_pval(c(0.051, 0.001), n = 3, alpha = c("0.05", "0.10"))
fmt_pval(0.03, alpha = 0.10)  # numeric alpha prints "<0.1"
}
\seealso{
\code{\link[=fmt_round]{fmt_round()}}
}
