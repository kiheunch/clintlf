% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_date.R
\name{impute_date}
\alias{impute_date}
\title{Impute partial dates to complete yyyy-mm-dd format}
\usage{
impute_date(
  x,
  fill_month = "01",
  fill_day = "01",
  fmt = c("ymd", "dmy", "mdy"),
  sep = NULL
)
}
\arguments{
\item{x}{Character vector of partial dates or datetimes, e.g.
\code{c("2025-07-19", "2025-07", "2025", "2026-05-22T15:30:45")}.}

\item{fill_month}{Month to impute when missing. Default \code{"01"}.}

\item{fill_day}{Day to impute when missing. Default \code{"01"}.}

\item{fmt}{Ordering of the input date components: \code{"ymd"} (default),
\code{"dmy"}, or \code{"mdy"}.}

\item{sep}{Separator between date components: \code{"-"}, \code{"/"}, or \code{"."}.
Auto-detected per element when \code{NULL} (default).}
}
\value{
Character vector of complete yyyy-mm-dd dates.
}
\description{
Fills in missing month and day components of partial date strings.
Handles \code{"ymd"} (ISO 8601), \code{"mdy"} (US), and \code{"dmy"} (European)
orderings, auto-detects the separator (\code{-}, \code{/}, \code{.}), and strips time
components before imputing.
}
\details{
Malformed or out-of-range output raises a warning, but calendar validity
is not checked: fill values can intentionally create dates such as
"2025-02-31". Validate with \code{as.Date()} when calendar-valid dates are
required.
}
\examples{
impute_date(c("2025-07-19", "2025-07", "2025", NA))
impute_date("2025", fill_month = "12", fill_day = "31")
impute_date("2025/07")                  # separator auto-detected
impute_date("07-19-2025", fmt = "mdy")
impute_date("2026-05-22T15:30:45")      # time stripped
}
