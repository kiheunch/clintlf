% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_round.R
\name{fmt_round}
\alias{fmt_round}
\title{Round half away from zero, formatted for display}
\usage{
fmt_round(x, n = 2L, eps = TRUE)
}
\arguments{
\item{x}{Numeric vector to round.}

\item{n}{Decimal places: a non-negative integer, scalar or one per element
of \code{x}. Default \code{2}.}

\item{eps}{Apply the scaled floating-point correction. Default \code{TRUE}.}
}
\value{
Character vector, same length as \code{x}, formatted to \code{n} decimal
places. \code{NA} and \code{NaN} become \code{"NA"} and \code{"NaN"}.
}
\description{
Rounds half away from zero (commercial rounding, as in SAS \code{round()};
base R rounds half to even) and returns character output with trailing
zeros kept, ready for table display. Negative values round symmetrically:
-2.5 becomes -3.
}
\details{
With \code{eps = TRUE}, a correction of \code{.Machine$double.eps * abs(x) * 10^n}
is added before truncation to offset binary representation error:
\code{abs(x)} tracks the floating-point spacing of the input, \code{10^n} the shift
to the rounding scale. This reproduces SAS \code{round()} on values stored just
under a half boundary, but can overcorrect when \code{abs(x)} or \code{n} is
extreme. A fixed correction such as \code{sqrt(.Machine$double.eps)} adapts to
neither.
}
\examples{
round(c(9.5, 10.5), 0)
fmt_round(c(9.5, 10.5), 0)

fmt_round(0.0001, n = 2)
fmt_round(0.1, n = 2)

fmt_round(c(2.5, 0.035, NA), c(0, 2, 2))
}
\references{
Based on:
\itemize{
\item \url{https://stackoverflow.com/questions/12688717/round-up-from-5}
\item \url{https://andrewlandgraf.com/2012/06/15/rounding-in-r/}
}

The author's scaled correction is proposed at
\url{https://stackoverflow.com/questions/12688717/round-up-from-5/79962354#79962354}.

For comparison in SAS, see: \code{round()}, \code{rounde()}
}
