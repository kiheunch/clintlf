% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_percent.R
\name{fmt_percent}
\alias{fmt_percent}
\title{Format a count with its percentage}
\usage{
fmt_percent(
  x,
  denom = NULL,
  n = 0L,
  perc_n = NULL,
  symbol = "\%",
  show_lt = TRUE,
  zero_denom = "-"
)
}
\arguments{
\item{x}{Numeric vector of counts or values.}

\item{denom}{Denominator for the percentage. Scalar or same length as \code{x}.
Defaults to \code{sum(x, na.rm = TRUE)}.}

\item{n}{Decimal places for the value. Default \code{0}.}

\item{perc_n}{Decimal places for the percentage. Defaults to \code{n}.}

\item{symbol}{Appended after the percentage. Default \code{"\%"}; use \code{""} to
omit.}

\item{show_lt}{Show a rounded-to-zero percentage of a positive \code{x} as its
\code{"<"} threshold. Default \code{TRUE}; set to \code{FALSE} for non-count input.}

\item{zero_denom}{Shown in place of the percentage when the denominator is
zero. Default \code{"-"}.}
}
\value{
Character vector in the form \code{"x (p\%)"}. \code{NA} formats as
\code{"NA (NA\%)"}; a zero denominator as \code{"x (-)"} (see \code{zero_denom}).
}
\description{
Combines a value and its percentage into \code{"x (p\%)"}, rounding half away
from zero. By default, a positive count whose percentage rounds to zero is
shown as a \code{"<"} threshold (e.g. \code{"<0.1\%"}), so a table never reports 0\%
for a non-zero count. A zero denominator leaves the percentage undefined;
those elements display \code{zero_denom} instead (e.g. \code{"0 (-)"}).
}
\examples{
fmt_percent(15, 100)
fmt_percent(15, 100, n = 1)
fmt_percent(c(30, 50, 20), perc_n = 1, symbol = "")
fmt_percent(0, 0)
fmt_percent(c(0, 0), zero_denom = "NA")
}
\seealso{
\code{\link[=fmt_round]{fmt_round()}}
}
